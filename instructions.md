# Brain Trainer - Тренажёр мозга для пожилых людей

## 📋 Описание проекта

Веб-приложение с набором мини-игр для тренировки когнитивных функций пожилых людей. Приложение оптимизировано для мобильных устройств и предоставляет простой, интуитивно понятный интерфейс.

## 🛠 Технологический стек

- **Frontend Framework**: React 18+
- **Language**: TypeScript
- **UI Framework**: Bootstrap 5
- **Build Tool**: Vite
- **Testing**: Vitest + React Testing Library
- **Deployment**: GitHub Pages (сборка в папку `docs`)

## 🎯 Ключевые требования

### UI/UX для пожилых пользователей
- **Крупные элементы**: кнопки минимум 60×60px
- **Контрастные цвета**: соотношение контраста минимум 4.5:1
- **Крупный шрифт**: базовый размер 18-20px
- **Простая навигация**: минимум вложенности, четкие действия
- **Без hover-эффектов**: все взаимодействия через клики/тапы
- **Mobile-first**: приоритет мобильной вёрстке
- **Минимум анимаций**: только необходимые для обратной связи

### Технические требования
- **Responsive**: корректная работа на экранах от 320px
- **Performance**: быстрая загрузка, оптимизация изображений
- **Accessibility**: базовые ARIA-атрибуты
- **State Management**: React Context или useState (без сложных библиотек)
- **LocalStorage**: сохранение результатов локально
- **PWA-ready**: возможность установки как приложение (опционально)

## 📁 Структура проекта

```
brain-trainer/
├── docs/                      # Папка для сборки (GitHub Pages)
├── public/
│   ├── favicon.ico
│   └── manifest.json
├── src/
│   ├── components/
│   │   ├── common/
│   │   │   ├── Header.tsx            # Заголовок с счётчиком очков
│   │   │   ├── Button.tsx            # Кастомная кнопка
│   │   │   ├── GameCard.tsx          # Карточка игры для меню
│   │   │   ├── GameLayout.tsx        # Общий layout для игр
│   │   │   ├── ResultsModal.tsx      # Модальное окно с результатами
│   │   │   └── ProgressBar.tsx       # Индикатор прогресса
│   │   ├── games/
│   │   │   ├── ReactionClick/
│   │   │   │   ├── ReactionClick.tsx
│   │   │   │   ├── ReactionClick.test.tsx
│   │   │   │   └── useReactionClick.ts
│   │   │   ├── ColorTap/
│   │   │   │   ├── ColorTap.tsx
│   │   │   │   ├── ColorTap.test.tsx
│   │   │   │   └── useColorTap.ts
│   │   │   ├── SymbolMatch/
│   │   │   ├── OddOneOut/
│   │   │   ├── HiddenNumber/
│   │   │   ├── MemoryFlip/
│   │   │   ├── SequenceRecall/
│   │   │   ├── DualRuleReaction/
│   │   │   ├── NBack/
│   │   │   └── LogicPairConcept/
│   │   └── GameMenu.tsx              # Главное меню выбора игр
│   ├── hooks/
│   │   ├── useScore.ts               # Хук для работы со счётом
│   │   ├── useScore.test.ts
│   │   ├── useLocalStorage.ts        # Хук для LocalStorage
│   │   ├── useLocalStorage.test.ts
│   │   ├── useTimer.ts               # Хук для таймера
│   │   └── useTimer.test.ts
│   ├── utils/
│   │   ├── gameUtils.ts              # Утилиты для игр
│   │   ├── gameUtils.test.ts
│   │   ├── randomUtils.ts            # Генерация случайных чисел
│   │   ├── randomUtils.test.ts
│   │   ├── emojiSets.ts              # Наборы эмодзи для игр
│   │   └── constants.ts              # Константы приложения
│   ├── types/
│   │   ├── game.types.ts             # Типы для игр
│   │   └── score.types.ts            # Типы для системы очков
│   ├── context/
│   │   └── ScoreContext.tsx          # Контекст для общего счёта
│   ├── styles/
│   │   ├── variables.scss            # Переменные (цвета, размеры)
│   │   └── global.scss               # Глобальные стили
│   ├── App.tsx
│   ├── App.test.tsx
│   ├── main.tsx
│   └── vite-env.d.ts
├── .gitignore
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 🎮 Подробное описание игр

### 1. Reaction Click
**Цель**: Тренировка скорости реакции

**Механика**:
- Показывается пустой экран с инструкцией "Ждите..."
- Через случайное время (1-4 сек) появляется крупная кнопка "Нажми!"
- Измеряется время от появления до нажатия
- 5 попыток подряд
- Результат: среднее время реакции в миллисекундах

**UI компоненты**:
- Полноэкранный контейнер
- Текст инструкции (центр экрана)
- Кнопка "Нажми!" (min 80×80px)
- Индикатор попытки (1/5)
- Результаты после каждой попытки

**Подсчёт очков**:
- < 300ms: 5 очков
- 300-500ms: 3 очка
- 500-800ms: 2 очка
- > 800ms: 1 очко

**Состояние**:
```typescript
interface ReactionState {
  status: 'waiting' | 'ready' | 'clicked' | 'results';
  currentAttempt: number;
  startTime: number | null;
  reactionTimes: number[];
}
```

---

### 2. Color Tap
**Цель**: Тренировка реакции + простая селекция

**Механика**:
- Показывается круг (зелёный или красный)
- Две кнопки: "ДА" и "НЕТ"
- Правило: зелёный = ДА, красный = НЕТ
- 20 раундов
- Измеряется точность и скорость

**UI компоненты**:
- Круг диаметром 150px (центр верхней половины)
- Две кнопки снизу (равной ширины)
- Счётчик раунда
- Индикатор правильных/неправильных ответов

**Подсчёт очков**:
- Правильный ответ: +1 очко
- Бонус за скорость < 1сек: +0.5 очка

**Состояние**:
```typescript
interface ColorTapState {
  currentRound: number;
  currentColor: 'green' | 'red';
  correctAnswers: number;
  startTime: number;
  results: { correct: boolean; time: number }[];
}
```

---

### 3. Symbol Match
**Цель**: Зрительное внимание

**Механика**:
- Показываются 2 эмодзи рядом
- Кнопки: "Совпадают" / "Не совпадают"
- 20 раундов
- Измеряется точность и время

**UI компоненты**:
- Два больших эмодзи (font-size: 80px)
- Две кнопки-ответа
- Счётчик раунда
- Прогресс-бар

**Наборы эмодзи**:
```typescript
const EMOJI_SET = ['😀', '😊', '🎉', '⭐', '🌟', '🎈', '🎁', '🍎', '🍊', '🌸'];
```

**Подсчёт очков**:
- Правильный ответ: +1 очко

**Состояние**:
```typescript
interface SymbolMatchState {
  currentRound: number;
  emoji1: string;
  emoji2: string;
  correctAnswers: number;
  startTime: number;
}
```

---

### 4. Odd One Out
**Цель**: Визуальный анализ

**Механика**:
- Сетка 2×2 с эмодзи
- 3 похожих, 1 отличается
- Пользователь тапает на лишнюю
- 10 раундов
- Сложность растёт (эмодзи становятся всё более похожие)

**UI компоненты**:
- Сетка 2×2 (каждая ячейка 100×100px)
- Эмодзи font-size: 60px
- Счётчик раунда
- Индикатор правильных ответов

**Логика сложности**:
```typescript
// Раунды 1-3: очень разные (😀 и 🚗)
// Раунды 4-7: средне похожие (😀 и 😊)
// Раунды 8-10: очень похожие (⭐ и 🌟)
```

**Подсчёт очков**:
- Правильный ответ: +1 очко

**Состояние**:
```typescript
interface OddOneOutState {
  currentRound: number;
  emojis: string[]; // массив из 4 эмодзи
  oddOneIndex: number; // индекс лишнего
  correctAnswers: number;
  startTime: number;
}
```

---

### 5. Hidden Number
**Цель**: Внимание и визуальный поиск

**Механика**:
- Сетка 5×6 (30 клеток)
- В случайной клетке появляется число
- Пользователь находит и нажимает
- 10 раундов
- Измеряется время поиска

**UI компоненты**:
- Сетка клеток (каждая ~50×50px)
- Клетки с фоном #f0f0f0
- Число в случайной клетке
- Таймер
- Счётчик раунда

**Подсчёт очков**:
- < 3 сек: 3 очка
- 3-5 сек: 2 очка
- > 5 сек: 1 очко

**Состояние**:
```typescript
interface HiddenNumberState {
  currentRound: number;
  targetPosition: number; // 0-29
  startTime: number;
  times: number[];
}
```

---

### 6. Memory Flip
**Цель**: Кратковременная память

**Механика**:
- Классическая игра "найди пары"
- Уровень 1: сетка 2×3 (6 карт, 3 пары)
- После успешного завершения уровня 1 автоматически начинается уровень 2
- Уровень 2: сетка 3×4 (12 карт, 6 пар)
- Карты с эмодзи
- Измеряется количество ходов и время

**UI компоненты**:
- Сетка карточек
- Карточка закрытая: серый фон с "?"
- Карточка открытая: эмодзи
- Счётчик ходов
- Таймер

**Логика**:
```typescript
interface Card {
  id: number;
  emoji: string;
  isFlipped: boolean;
  isMatched: boolean;
}
```

**Подсчёт очков**:
- Уровень 1: базовый балл 10, минус 1 за каждый лишний ход сверх 5
- Уровень 2: базовый балл 20, минус 1 за каждый лишний ход сверх 10

**Состояние**:
```typescript
interface MemoryFlipState {
  level: 1 | 2;
  cards: Card[];
  flippedCards: number[]; // индексы открытых карт
  matchedPairs: number;
  moves: number;
  startTime: number;
}
```

---

### 7. Sequence Recall
**Цель**: Визуальная рабочая память

**Механика**:
- Показывается последовательность эмодзи
- Начало: 3 элемента
- Каждый эмодзи показывается 800ms, затем пауза 200ms
- Пользователь воспроизводит порядок, нажимая на кнопки
- При успехе: +1 элемент к следующей последовательности (макс 7)
- При ошибке: конец игры
- Максимум попыток: до ошибки или достижения длины 7

**UI компоненты**:
- Фаза показа: эмодзи появляются по очереди в центре экрана (крупно)
- Фаза ввода: сетка кнопок с 8 разными эмодзи (вариантами для выбора)
- Индикатор текущей позиции ввода (например, "2 из 4")
- Текущая длина последовательности

**Подсчёт очков**:
- Успешная последовательность: +количество элементов в очки

**Состояние**:
```typescript
interface SequenceRecallState {
  sequence: string[];
  userSequence: string[];
  currentLength: number;
  phase: 'showing' | 'input' | 'result';
  showingIndex: number;
}
```

---

### 8. Dual-Rule Reaction
**Цель**: Когнитивная гибкость (переключение правил)

**Механика**:
- Показывается фигура (круг/квадрат) с цветом (зелёный/красный)
- Кнопки: "A" и "B"
- Раунды 1-15: правило по форме (круг → A, квадрат → B)
- Раунды 16-30: правило по цвету (зелёный → A, красный → B)
- Смена правила происходит в раунде 16 БЕЗ предупреждения (пользователь должен сам догадаться)
- Измеряются ошибки и задержки

**UI компоненты**:
- Фигура (150×150px) в центре
- Две кнопки: "A" и "B"
- Текст с правилом показывается только в раунде 1: "Круг → A, Квадрат → B"
- Счётчик раунда
- Индикатор ошибок

**Подсчёт очков**:
- Правильный ответ: +1 очко
- Штраф за ошибку: -0.5 очка

**Состояние**:
```typescript
interface DualRuleState {
  currentRound: number;
  shape: 'circle' | 'square';
  color: 'green' | 'red';
  currentRule: 'shape' | 'color';
  errors: number;
  startTime: number;
}
```

---

### 9. N-Back (2-back)
**Цель**: Рабочая память и обновление информации

**Механика**:
- Поток эмодзи появляется по одному (каждые 2.5 сек)
- Кнопка "Совпадает"
- Нажимать, если текущий = тому, что был 2 шага назад
- 3 блока по 20 символов (всего 60)
- Между блоками пауза 3 секунды с сообщением "Блок X из 3"
- Измеряется точность

**UI компоненты**:
- Текущий эмодзи (крупно, 100px)
- Кнопка "Совпадает"
- История (опционально: показывать 2 предыдущих, но меньшего размера)
- Прогресс блока
- Счётчик правильных/пропущенных

**Логика n-back**:
```typescript
// Пример: 🍎 🍊 🍎 (нажать) 🍊 (нажать) 🌟 ...
// В позиции 2 (индекс 2): текущий=🍎, проверяем индекс 0 = 🍎 → совпадает
```

**Подсчёт очков**:
- Правильное нажатие: +1 очко
- Правильный пропуск: +0.5 очка
- Ошибка: 0 очков

**Состояние**:
```typescript
interface NBackState {
  sequence: string[];
  currentIndex: number;
  currentBlock: number;
  hits: number;
  misses: number;
  falseAlarms: number;
}
```

---

### 10. Logic Pair Concept
**Цель**: Абстрактное мышление и логическое обобщение

**Механика**:
- Показываются 4 предмета (текст/эмодзи)
- Пользователь выбирает 2, образующие смысловую пару
- 10 раундов с разными категориями
- Проверка правильности

**Наборы данных**:
```typescript
const ROUNDS = [
  {
    items: ['🍎 Яблоко', '🍊 Апельсин', '🐕 Собака', '🍌 Банан'],
    correctPairs: [[0, 1], [0, 3], [1, 3]], // любые 2 фрукта
    category: 'фрукты'
  },
  {
    items: ['⚽️ Футбол', '🏀 Баскетбол', '📚 Книга', '🎹 Пианино'],
    correctPairs: [[0, 1]],
    category: 'спорт'
  },
  {
    items: ['🐕 Собака', '🐈 Кошка', '🚗 Машина', '🌳 Дерево'],
    correctPairs: [[0, 1]],
    category: 'животные'
  },
  {
    items: ['☀️ Солнце', '🌙 Луна', '🍕 Пицца', '⭐ Звезда'],
    correctPairs: [[0, 1], [0, 3], [1, 3]],
    category: 'небесные тела'
  },
  {
    items: ['👔 Рубашка', '👖 Брюки', '🍞 Хлеб', '🥐 Круассан'],
    correctPairs: [[0, 1], [2, 3]],
    category: 'одежда или еда'
  },
  {
    items: ['🎸 Гитара', '🎹 Пианино', '✏️ Карандаш', '🖊️ Ручка'],
    correctPairs: [[0, 1], [2, 3]],
    category: 'инструменты музыкальные или письменные'
  },
  {
    items: ['🌹 Роза', '🌻 Подсолнух', '🦅 Орёл', '🦋 Бабочка'],
    correctPairs: [[0, 1], [2, 3]],
    category: 'растения или живые существа'
  },
  {
    items: ['🏠 Дом', '🏢 Здание', '🍔 Бургер', '🌮 Тако'],
    correctPairs: [[0, 1], [2, 3]],
    category: 'строения или еда'
  },
  {
    items: ['📱 Телефон', '💻 Ноутбук', '👞 Ботинки', '👟 Кроссовки'],
    correctPairs: [[0, 1], [2, 3]],
    category: 'техника или обувь'
  },
  {
    items: ['🌊 Море', '🏔️ Гора', '🌋 Вулкан', '🏝️ Остров'],
    correctPairs: [[0, 3], [1, 2]],
    category: 'природные объекты'
  },
];
```

**UI компоненты**:
- Сетка 2×2 с предметами (кнопки)
- Выбранные элементы подсвечиваются
- Кнопка "Подтвердить" (активна при выборе 2х)
- Счётчик раунда
- Обратная связь (правильно/неправильно)

**Подсчёт очков**:
- Правильная пара: +2 очка

**Состояние**:
```typescript
interface LogicPairConceptState {
  currentRound: number;
  selectedItems: number[]; // индексы выбранных элементов (max 2)
  correctAnswers: number;
  currentRoundData: {
    items: string[];
    correctPairs: number[][];
    category: string;
  };
}
```

---

## 🏆 Система подсчёта очков

### Глобальный счёт
- Суммируются очки из всех пройденных игр
- Отображается в Header
- Сохраняется в LocalStorage
- Счёт накапливается бесконечно (не сбрасывается автоматически)
- Пользователь может сбросить счёт вручную через настройки/меню

### Интерфейс ScoreContext
```typescript
interface ScoreContextType {
  totalScore: number;
  gameScores: Record<string, number>;
  addScore: (gameId: string, points: number) => void;
  resetScore: () => void;
  getGameScore: (gameId: string) => number;
}
```

### Сохранение результатов
```typescript
interface GameResult {
  gameId: string;
  score: number;
  date: string;
  details: any; // специфичные для игры данные
}

// LocalStorage key: 'brain-trainer-results'
```

---

## 🧭 Навигация и UX Flow

### Главный экран (GameMenu)
- Список всех 10 игр (карточки)
- Каждая карточка:
  - Название игры
  - Иконка/эмодзи
  - Краткое описание
  - Лучший результат
  - Кнопка "Играть"

### Экран игры
- Header:
  - Кнопка "Назад в меню"
  - Общий счёт
  - Название игры
- Область игры (зависит от игры)
- Footer (опционально):
  - Прогресс
  - Подсказка

### После завершения игры
- Модальное окно с результатами:
  - Заработано очков
  - Статистика (точность, время)
  - Кнопки:
    - "Играть ещё раз"
    - "Следующая игра"
    - "В меню"

### Возможность выйти в процессе
- Кнопка "Назад" всегда доступна
- При нажатии - предупреждение: "Прогресс будет потерян. Выйти?"
- Набранные очки сохраняются

### Техническая реализация навигации
Используется простое состояние в `App.tsx` без React Router:
```typescript
type Screen = 'menu' | 'game';

interface AppState {
  currentScreen: Screen;
  currentGame: string | null; // ID игры
}
```

**Логика**:
- `App.tsx` управляет состоянием навигации
- При клике на игру в меню: `setCurrentGame(gameId)` и `setCurrentScreen('game')`
- При выходе из игры: `setCurrentScreen('menu')` и `setCurrentGame(null)`
- Условный рендеринг: `{currentScreen === 'menu' ? <GameMenu /> : <CurrentGameComponent />}`

---

## 🎨 Responsive Design и Breakpoints

### Breakpoints
```scss
// variables.scss
$breakpoint-xs: 320px;  // маленькие телефоны
$breakpoint-sm: 576px;  // телефоны
$breakpoint-md: 768px;  // планшеты
$breakpoint-lg: 992px;  // десктопы
$breakpoint-xl: 1200px; // большие экраны
```

### Стратегия адаптивности
1. **320px - 575px** (приоритет):
   - Одноколоночный layout
   - Кнопки на всю ширину
   - Шрифт 18px
   - Эмодзи scaled down если нужно

2. **576px - 767px**:
   - Можно показывать элементы в 2 колонки (карточки игр)
   - Шрифт 20px
   - Больше padding

3. **768px+**:
   - Максимальная ширина контейнера игры 600px (центрирование)
   - Карточки меню в 2-3 колонки
   - Добавление margin по бокам

### Правила
- Все размеры в `rem` или `%`, кроме минимальных границ
- `min-height` для кнопок, а не фиксированная высота
- `max-width` для контейнеров игр
- Тестирование на реальных устройствах

---

## 🧪 Тестирование

### Требования к покрытию
- **Utilities**: 100% покрытие
- **Hooks**: 100% покрытие
- **Components**: минимум 80%

### Примеры тестов

#### Хуки (useScore.test.ts)
```typescript
describe('useScore', () => {
  it('should initialize with 0 score', () => {});
  it('should add score correctly', () => {});
  it('should save to localStorage', () => {});
  it('should load from localStorage', () => {});
  it('should reset score', () => {});
});
```

#### Утилиты (randomUtils.test.ts)
```typescript
describe('getRandomInt', () => {
  it('should return number in range', () => {});
  it('should handle edge cases', () => {});
});

describe('shuffleArray', () => {
  it('should shuffle array', () => {});
  it('should not modify original', () => {});
  it('should preserve all elements', () => {});
});
```

#### Компоненты игр
```typescript
describe('ReactionClick', () => {
  it('should render waiting state', () => {});
  it('should show button after delay', () => {});
  it('should measure reaction time', () => {});
  it('should complete after 5 attempts', () => {});
  it('should calculate average time', () => {});
});
```

### Запуск тестов
```bash
npm run test          # запуск всех тестов
npm run test:ui       # UI для тестов
npm run test:coverage # отчёт о покрытии
```

---

## ⚠️ Обработка ошибок и Edge Cases

### LocalStorage
- Проверка доступности LocalStorage при инициализации
- Fallback на in-memory хранилище если недоступен
- Обработка ошибок при чтении/записи (try-catch)
- Показ предупреждения пользователю если данные не могут быть сохранены

### Некорректные состояния игр
- Валидация индексов массивов перед доступом
- Проверка таймеров перед использованием
- Graceful degradation при некорректных данных

### Производительность
- Debounce для частых действий (если необходимо)
- Очистка таймеров в useEffect cleanup
- Memo для тяжелых компонентов (если необходимо)

### Accessibility
- Все интерактивные элементы доступны с клавиатуры
- ARIA-labels для кнопок без текста
- `role` атрибуты для семантики
- Цветовая доступность (не только цвет для информации)

---

## 📋 Константы и данные приложения

### emojiSets.ts
```typescript
// Для Symbol Match - разнообразные, легко различимые
export const SYMBOL_MATCH_EMOJIS = [
  '😀', '😊', '😂', '🥳', '😎',
  '🎉', '🎈', '🎁', '⭐', '🌟',
  '🍎', '🍊', '🍌', '🍇', '🍓',
  '🌸', '🌻', '🌹', '🌺', '🌷',
  '🚗', '✈️', '🚀', '⚽', '🎸',
];

// Для Odd One Out - категории по сложности
export const ODD_ONE_OUT_EMOJIS = {
  easy: {
    // Очень разные категории
    sets: [
      ['😀', '😀', '😀', '🚗'],
      ['🍎', '🍎', '🍎', '⚽'],
      ['🌸', '🌸', '🌸', '🏠'],
    ],
  },
  medium: {
    // Похожие из одной категории
    sets: [
      ['😀', '😀', '😀', '😊'],
      ['🍎', '🍎', '🍎', '🍊'],
      ['🚗', '🚗', '🚗', '🚕'],
    ],
  },
  hard: {
    // Очень похожие
    sets: [
      ['⭐', '⭐', '⭐', '🌟'],
      ['🌕', '🌕', '🌕', '🌖'],
      ['😊', '😊', '😊', '😄'],
    ],
  },
};

// Для Memory Flip, Sequence Recall, N-Back
export const MEMORY_GAME_EMOJIS = [
  '🐶', '🐱', '🐭', '🐹', '🐰', '🦊',
  '🐻', '🐼', '🐨', '🐯', '🦁', '🐮',
  '🐷', '🐸', '🐵', '🐔', '🐧', '🐦',
  '🌺', '🌸', '🌼', '🌻', '🌹', '🌷',
  '🍎', '🍊', '🍋', '🍌', '🍉', '🍇',
];

// Для Sequence Recall - выбор вариантов (показывается пользователю)
export const SEQUENCE_OPTIONS = [
  '🎈', '🎁', '🎉', '⭐', '🌟', '🎨', '🎭', '🎪',
];

// Для N-Back - отдельный пул для генерации последовательности
export const N_BACK_EMOJIS = [
  '🔴', '🔵', '🟢', '🟡', '🟣', '🟠',
  '⬛', '⬜', '🟥', '🟦', '🟩', '🟨',
];
```

### constants.ts
```typescript
// Идентификаторы игр
export const GAME_IDS = {
  REACTION_CLICK: 'reaction-click',
  COLOR_TAP: 'color-tap',
  SYMBOL_MATCH: 'symbol-match',
  ODD_ONE_OUT: 'odd-one-out',
  HIDDEN_NUMBER: 'hidden-number',
  MEMORY_FLIP: 'memory-flip',
  SEQUENCE_RECALL: 'sequence-recall',
  DUAL_RULE: 'dual-rule-reaction',
  N_BACK: 'n-back',
  LOGIC_PAIR: 'logic-pair-concept',
} as const;

// Метаданные игр
export const GAMES_META = [
  {
    id: GAME_IDS.REACTION_CLICK,
    title: 'Reaction Click',
    description: 'Тренировка скорости реакции',
    icon: '⚡',
    difficulty: 1,
  },
  {
    id: GAME_IDS.COLOR_TAP,
    title: 'Color Tap',
    description: 'Реакция и селекция',
    icon: '🎨',
    difficulty: 1,
  },
  // ... остальные игры
];

// Тайминги
export const TIMINGS = {
  REACTION_MIN: 1000,
  REACTION_MAX: 4000,
  SEQUENCE_SHOW: 800,
  SEQUENCE_PAUSE: 200,
  N_BACK_INTERVAL: 2500,
  BLOCK_PAUSE: 3000,
} as const;

// Размеры сеток
export const GRID_SIZES = {
  HIDDEN_NUMBER: { rows: 5, cols: 6 },
  ODD_ONE_OUT: { rows: 2, cols: 2 },
  MEMORY_FLIP_L1: { rows: 2, cols: 3 },
  MEMORY_FLIP_L2: { rows: 3, cols: 4 },
} as const;

// Количество раундов
export const ROUNDS = {
  REACTION_CLICK: 5,
  COLOR_TAP: 20,
  SYMBOL_MATCH: 20,
  ODD_ONE_OUT: 10,
  HIDDEN_NUMBER: 10,
  DUAL_RULE: 30,
  N_BACK_PER_BLOCK: 20,
  N_BACK_BLOCKS: 3,
  LOGIC_PAIR: 10,
} as const;

// LocalStorage ключи
export const STORAGE_KEYS = {
  TOTAL_SCORE: 'brain-trainer-score',
  GAME_SCORES: 'brain-trainer-game-scores',
  RESULTS_HISTORY: 'brain-trainer-results',
} as const;
```

---

## 🏗 Архитектура компонентов

### Общие паттерны

#### Кастомный хук для игры
Каждая игра должна иметь хук `useGameName`:
```typescript
function useReactionClick() {
  const [state, setState] = useState<GameState>(initialState);
  
  const start = () => { /* ... */ };
  const handleClick = () => { /* ... */ };
  const reset = () => { /* ... */ };
  
  return {
    state,
    start,
    handleClick,
    reset,
    isComplete: state.status === 'complete',
    score: calculateScore(state),
  };
}
```

#### Компонент игры
```typescript
function GameName() {
  const { state, handleClick, score, isComplete } = useGameName();
  const { addScore } = useScore();
  
  useEffect(() => {
    if (isComplete) {
      addScore('game-name', score);
    }
  }, [isComplete]);
  
  return (
    <GameLayout title="Название игры">
      {/* игровой контент */}
    </GameLayout>
  );
}
```

### Структура App.tsx
```typescript
import { useState } from 'react';
import { ScoreProvider } from './context/ScoreContext';
import Header from './components/common/Header';
import GameMenu from './components/GameMenu';
import ReactionClick from './components/games/ReactionClick/ReactionClick';
// ... импорты других игр
import { GAME_IDS } from './utils/constants';

type GameId = typeof GAME_IDS[keyof typeof GAME_IDS] | null;

function App() {
  const [currentGame, setCurrentGame] = useState<GameId>(null);

  const handleGameSelect = (gameId: GameId) => {
    setCurrentGame(gameId);
  };

  const handleBackToMenu = () => {
    setCurrentGame(null);
  };

  const renderGame = () => {
    switch (currentGame) {
      case GAME_IDS.REACTION_CLICK:
        return <ReactionClick onExit={handleBackToMenu} />;
      case GAME_IDS.COLOR_TAP:
        return <ColorTap onExit={handleBackToMenu} />;
      // ... остальные игры
      default:
        return null;
    }
  };

  return (
    <ScoreProvider>
      <div className="app">
        <Header showBackButton={currentGame !== null} onBack={handleBackToMenu} />
        {currentGame === null ? (
          <GameMenu onGameSelect={handleGameSelect} />
        ) : (
          renderGame()
        )}
      </div>
    </ScoreProvider>
  );
}

export default App;
```

---

## 🎨 Дизайн-система

### Цветовая палитра
```scss
// variables.scss
$primary: #4CAF50;      // зелёный для успеха
$secondary: #2196F3;    // синий для информации
$danger: #f44336;       // красный для ошибок
$warning: #FF9800;      // оранжевый для предупреждений
$light: #f5f5f5;        // фон
$dark: #333333;         // текст
$gray: #757575;         // второстепенный текст

// Для игр
$game-green: #4CAF50;
$game-red: #f44336;
```

### Размеры
```scss
$font-size-base: 20px;
$font-size-large: 24px;
$font-size-xl: 32px;

$btn-min-height: 60px;
$btn-min-width: 120px;
$btn-border-radius: 12px;

$emoji-size-small: 48px;
$emoji-size-medium: 80px;
$emoji-size-large: 120px;
```

### Компонент Button
```typescript
interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'success' | 'danger';
  size?: 'small' | 'medium' | 'large';
  fullWidth?: boolean;
  disabled?: boolean;
  onClick: () => void;
  children: React.ReactNode;
}
```

---

## ⚙️ Настройка проекта

### package.json (скрипты)
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --outDir docs --base=/brain-trainer/",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "lint": "eslint src --ext ts,tsx",
    "format": "prettier --write src/**/*.{ts,tsx,scss}"
  }
}
```

### vite.config.ts
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  base: '/brain-trainer/', // для GitHub Pages
  build: {
    outDir: 'docs',
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/setup.ts',
    coverage: {
      provider: 'v8',
      reporter: ['text', 'html'],
      exclude: ['node_modules/', 'src/test/'],
    },
  },
});
```

### tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

---

## 📝 Конфигурационные файлы

### .gitignore
```
# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage

# Production (НЕ игнорируем docs/ так как это наша сборка для GitHub Pages)
# docs/ - закоммитим

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
```

### .eslintrc.cjs (опционально)
```javascript
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs', 'docs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
  },
};
```

### index.html
```html
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Тренажёр мозга для пожилых людей" />
    <title>Brain Trainer</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

---

## 📦 Зависимости

### Основные
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "bootstrap": "^5.3.0",
    "react-bootstrap": "^2.9.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "@testing-library/react": "^14.1.0",
    "@testing-library/jest-dom": "^6.1.0",
    "@testing-library/user-event": "^14.5.0",
    "@vitest/ui": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "sass": "^1.69.0"
  }
}
```

---

## 🚀 Деплой на GitHub Pages

### Шаги
1. Собрать проект: `npm run build`
2. Закоммитить папку `docs/`
3. В настройках репозитория: Settings → Pages → Source: `main branch /docs folder`
4. URL будет: `https://username.github.io/brain-trainer/`

### GitHub Actions (опционально)
Можно настроить автоматическую сборку при пуше в main.

---

## 📝 План реализации (порядок разработки)

### Этап 1: Базовая инфраструктура
1. Инициализация проекта (Vite + React + TypeScript)
2. Установка зависимостей
3. Настройка Vitest
4. Создание структуры папок
5. Базовые типы (`game.types.ts`, `score.types.ts`)
6. Настройка Bootstrap + кастомные переменные

### Этап 2: Общие компоненты
1. `Button` + тесты
2. `Header` с отображением счёта
3. `GameLayout`
4. `GameCard` для меню
5. `ResultsModal`
6. `ProgressBar`

### Этап 3: Утилиты и хуки
1. `randomUtils.ts` + тесты
2. `gameUtils.ts` + тесты
3. `emojiSets.ts`
4. `useLocalStorage` + тесты
5. `useTimer` + тесты
6. `useScore` + тесты
7. `ScoreContext`

### Этап 4: Главное меню
1. `GameMenu` с карточками игр
2. Навигация между меню и играми
3. Роутинг (опционально react-router или просто состояние)

### Этап 5: Реализация игр (по порядку)
1. **Reaction Click** (самая простая)
2. **Color Tap**
3. **Symbol Match**
4. **Odd One Out**
5. **Hidden Number**
6. **Memory Flip**
7. **Sequence Recall**
8. **Dual-Rule Reaction**
9. **N-Back** (сложная)
10. **Logic Pair Concept**

### Этап 6: Интеграция и полировка
1. Проверка всех игр
2. Модальные окна с результатами
3. Навигация "Следующая игра"
4. Сохранение/загрузка из LocalStorage
5. Адаптивность (тестирование на разных экранах)
6. Accessibility (ARIA-атрибуты)

### Этап 7: Тестирование и оптимизация
1. Запуск всех тестов
2. Проверка coverage
3. Исправление багов
4. Оптимизация производительности
5. Lighthouse audit

### Этап 8: Деплой
1. Настройка build конфигурации
2. Сборка в `docs/`
3. Пуш в GitHub
4. Настройка GitHub Pages
5. Проверка работы на продакшене

---

## 🎯 Критерии успеха

- ✅ Все 10 игр реализованы и работают
- ✅ Система подсчёта очков функционирует
- ✅ Результаты сохраняются локально
- ✅ Приложение адаптивно (корректно на 320px - 1920px)
- ✅ Все утилиты и хуки покрыты тестами
- ✅ Проект успешно собирается в `docs/`
- ✅ Деплой на GitHub Pages работает
- ✅ Интерфейс понятен пожилым пользователям
- ✅ Нет hover-эффектов, все взаимодействия через клики
- ✅ Крупные шрифты и кнопки
- ✅ Высокая контрастность

---

## ❓ FAQ (Часто задаваемые вопросы)

### Нужен ли React Router?
Нет, используется простое состояние в App.tsx. Это упрощает навигацию для такого небольшого приложения.

### Почему сборка в папку `docs/`, а не `dist/`?
GitHub Pages позволяет деплоить из папки `docs/` на main ветке, что удобнее чем настройка gh-pages ветки.

### Нужны ли анимации переходов между играми?
Минимальные. Для пожилых пользователей важнее простота и понятность. Можно добавить простой fade-in, но без сложных transitions.

### Как обрабатывать случайность в тестах?
Использовать моки для функций генерации случайных чисел. Например:
```typescript
vi.spyOn(Math, 'random').mockReturnValue(0.5);
```

### Нужна ли локализация?
На первом этапе - нет. Приложение на русском. Локализацию можно добавить позже как улучшение.

### Что делать с результатами после переустановки браузера?
Результаты хранятся в LocalStorage и будут потеряны. Это приемлемо для MVP. В будущем можно добавить экспорт/импорт или облачное хранение.

### Как тестировать на мобильных устройствах?
1. Chrome DevTools с режимом мобильных устройств
2. Vite позволяет открывать dev-сервер по локальной сети
3. Тестирование на реальных устройствах после деплоя

### Нужна ли поддержка старых браузеров?
Целевые браузеры: современные Chrome, Safari, Firefox (последние 2 версии). Vite по умолчанию генерирует современный код, что приемлемо для целевой аудитории со смартфонами.

---

## 📊 Итоговая сводка

### Что покрывает спецификация:
✅ Полное описание всех 10 игр с механиками  
✅ TypeScript интерфейсы для состояний  
✅ Система подсчёта очков  
✅ UI/UX требования для пожилых пользователей  
✅ Навигация и архитектура приложения  
✅ Константы, эмодзи-наборы, конфигурации  
✅ План тестирования и требования к покрытию  
✅ Responsive дизайн и breakpoints  
✅ Обработка ошибок и edge cases  
✅ Конфигурационные файлы (vite, tsconfig, gitignore)  
✅ План разработки по этапам  
✅ Инструкции по деплою  

### Готовность к разработке: 100%
Спецификация содержит всю необходимую информацию для начала и завершения разработки проекта.

---

## 🔮 Возможные улучшения (future)

- Добавить уровни сложности для каждой игры
- Статистика по дням/неделям
- Графики прогресса
- PWA с оффлайн-режимом
- Звуковые эффекты (опционально)
- Мультиязычность
- Экспорт результатов
- Социальные функции (сравнение с друзьями)
- Персональные рекомендации игр
- Адаптивные уровни сложности на основе результатов
- Темная тема (опционально)

---

## 📄 Лицензия

MIT

---

## 👨‍💻 Автор

Brain Trainer - проект для помощи пожилым людям в тренировке когнитивных функций.

---

**Готово к разработке!** Следуйте плану реализации, и вы получите полнофункциональное приложение.

Эта спецификация является живым документом и может дополняться по мере развития проекта.

